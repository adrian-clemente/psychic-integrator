<div class="releaseProjectInfo">
    <span class="releaseProjectTitle">Project:</span>
    <span class="releaseProjectData">{{ .Project }}</span>
    <span class="releaseProjectTitle">Version:</span>
    <span class="releaseProjectData">{{.Version}}</span>
    <div class="commitSectionHeader">Commits:</div>
</div>

<div class="commitsSection">
    {{ if .ReleaseCommitsElements }}
        {{ .ReleaseCommitsElements }}
    {{else}}
        <div class="noCommit">There isn't anything to commit</div>
    {{ end }}
<div>